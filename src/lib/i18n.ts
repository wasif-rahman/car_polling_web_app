/**
 * Simple translation helper for internationalization.
 * Currently returns the key itself as the default English text.
 * Can be expanded in the future to support multiple languages using a dictionary.
 */
export function t(key: string): string {
  return key;
}
